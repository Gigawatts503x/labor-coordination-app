// backend/config/env.example
// Copy this to .env and fill in your values
// THIS FILE: Shows what environment variables you need

# Database Type: "sqlite" for local development, "postgres" for Synology/production
DB_TYPE=sqlite

# Only needed if DB_TYPE=postgres:
DB_USER=labor_admin
DB_PASSWORD=YourSecurePassword123
DB_HOST=localhost
DB_PORT=5432
DB_NAME=labor_coordinator

# Server
NODE_ENV=development
PORT=3001

# File paths
DATA_DIR=./data
UPLOADS_DIR=./uploads
